# AI Code Generator

A simple Streamlit application that uses the OpenRouter API (StepFun 3.5 Flash) to generate code snippets.

## Setup

1. **Install Dependencies:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Run the App:**
   ```bash
   streamlit run app.py
   ```

## Configuration

The API key is stored in `.env`.
